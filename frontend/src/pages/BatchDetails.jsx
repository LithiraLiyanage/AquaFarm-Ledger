import React from 'react';
import { useEffect, useState } from 'react';import { useParams } from 'react-router-dom';import { api } from '../services/api';import { Card, LoadingSpinner, PageHeader, StatusBadge } from '../components/ui';
export default function BatchDetails(){const {id}=useParams();const [b,setB]=useState(null);useEffect(()=>{api.get('/batches/'+id).then(r=>setB(r.data.data))},[id]);if(!b)return <LoadingSpinner/>;const survival=b.initialStock?((b.currentStock/b.initialStock)*100).toFixed(1):0;const biomass=(b.currentStock*b.averageWeight).toFixed(2);return <><PageHeader title={b.batchName} subtitle={`${b.species} • ${b.pond?.name||'Pond'}`}/><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-4"><Card><p>Status</p><StatusBadge type={b.status}>{b.status}</StatusBadge></Card><Card><p className="text-sm text-slate-500">Survival Rate</p><h3 className="text-3xl font-black">{survival}%</h3></Card><Card><p className="text-sm text-slate-500">Biomass</p><h3 className="text-3xl font-black">{biomass} kg</h3></Card><Card><p className="text-sm text-slate-500">Current Stock</p><h3 className="text-3xl font-black">{b.currentStock}</h3></Card></div><Card className="mt-6"><h3 className="text-xl font-black">Growth Timeline</h3><div className="mt-6 grid grid-cols-4 gap-3">{['Stocked','Growing','Monitoring','Harvest'].map((x,i)=><div className="rounded-2xl bg-white p-4 text-center"><div className="mx-auto mb-2 h-4 w-4 rounded-full bg-purple"/><b>{x}</b></div>)}</div></Card></>}
