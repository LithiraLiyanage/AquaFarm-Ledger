import React from 'react';
import { useEffect, useState } from 'react';import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { api } from '../services/api';import { Button, Card, LoadingSpinner, PageHeader } from '../components/ui';
export default function Reports(){const [d,setD]=useState(null);useEffect(()=>{api.get('/reports/dashboard').then(r=>setD(r.data.data))},[]);if(!d)return <LoadingSpinner/>;return <><PageHeader title="Reports" subtitle="Pond performance, batch survival, feed usage, water quality, cost and harvest forecasts." action={<div className="flex gap-2"><Button variant="ghost">Export CSV</Button><Button>Export PDF</Button></div>}/><div className="grid gap-6 lg:grid-cols-2"><Chart title="Cost by Category" data={d.charts.costByCategory} dataKey="value"/><Chart title="Harvest Profit Forecast" data={d.charts.harvestForecast} dataKey="profit"/><Chart title="Feed Usage Report" data={d.charts.feedUsage} dataKey="kg"/><Chart title="Mortality Report" data={d.charts.mortalityTrend} dataKey="deaths"/></div></>}
function Chart({title,data,dataKey}){return <Card><h3 className="mb-4 text-xl font-black">{title}</h3><div className="h-72"><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey={dataKey} fill="#7C3AED" radius={[12,12,0,0]}/></BarChart></ResponsiveContainer></div></Card>}
